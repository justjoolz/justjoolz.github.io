# Site content

`music.json` is the editable catalogue used by the Music page. Add or revise entries there without changing the page layout.

Dates and credits may use plain uncertainty markers such as `c. 2020`, `Recent`, or `Date to verify` rather than false precision.
